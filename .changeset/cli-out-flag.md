---
"@maastrich/hashup": minor
---

Add `-o, --out <path>` flag to the `hashup` CLI. Writes the output to the given file (creating parent directories as needed) instead of stdout. Works in every mode: single-file, config, and `--print-schema`.
